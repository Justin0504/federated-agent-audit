services:
  audit-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN:-}
    command: ["server", "--host", "0.0.0.0", "--port", "8000"]
    restart: unless-stopped
