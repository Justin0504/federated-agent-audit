# Submission draft

One self-contained paper merging the multi-tenant A2A contribution (lead) with
the single-tenant federated/DP-robust engine (validated on AgentLeak) as its
foundation. Sources: `research/A2A_MULTITENANT_PRIVACY.md` (design),
`paper/A2A_MT_PAPER.md` (markdown method/eval), `paper/OUTLINE.md` (single-tenant
predecessor).

## Build
```bash
tectonic main.tex      # -> main.pdf (5 pp.)
```

## Venue plan
- **arXiv first** to stake the "federated + DP-robust multi-tenant auditor + A2A
  extension" claim quickly (the space moves fast).
- Then a privacy venue — **PoPETs/PETS** (rolling deadlines, privacy fit) is the
  best target; alternatively USENIX Security / CCS, or an LLM-agent / trustworthy-ML
  workshop for a fast first stake.
